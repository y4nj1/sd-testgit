this is my first git file, don't mess with it!
#to stage a file
git add <filename>
#to commit a file
git commit -m "message"
#to list status
git status

#to add remote origin
git remote add origin https://github.com/y4nj1/sd-testgit

#to push committed files from local to remote
git push -u origin <branchname>